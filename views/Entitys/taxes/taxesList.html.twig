{% extends "menu.html.twig" %}
{% block content %}
    <div class="container">
        <div class="row">
            <div class="col-md-10">               
                <h2> Tipos de Iva </h2>
            </div>   
            <div class="col-md-2 d-flex justify-content-end align-items-end">
                <a class="link " href="/Intranet/admin?selected_tab=home">Volver</a> 
            </div>
        </div>        
        <div class="row">
            <div class="col">
                <table class="table">
                    <tr>
                        <th>ID</th>
                        <th>Nombre</th>
                        <th>Porcentaje</th>                        
                        <th>Edit</th>
                        <th>Delete</th>
                    </tr>
                    {% for taxe in taxes %}
                        <tr>
                            <td>{{ taxe.id }}
                            <td>{{ taxe.name }}
                            <td>{{ taxe.percentaje }}</td>                           
                            <td><a href="/Intranet/taxes/form?id={{taxe.id}}">Edit</a></td>
                            <td><a href="/Intranet/taxes/delete?id={{taxe.id}}">Delete</a></td>
                        </tr>
                    {% endfor %}                
                </table>
            </div>
        </div>
        <div class="row">
            <div class="col d-flex justify-content-end">
                <form id="addTaxes" action="/Intranet/taxes/form" method="get">
                    <div class="form-group">
                        <input id="submit" type="submit" class="btn btn-primary" value="Añadir">
                        <a href="/Intranet/admin?selected_tab=home" class="btn btn-primary">Cancelar</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock %}

